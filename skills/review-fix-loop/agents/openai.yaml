interface:
  display_name: "Review Fix Loop"
  short_description: "Loop Codex review fixes while staying scoped to the issue or PR."
  default_prompt: "Use $review-fix-loop to run /review, compare findings against the issue or PR scope, fix actionable in-scope findings, and rerun until clean."
